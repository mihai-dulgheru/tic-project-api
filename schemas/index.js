const coachSchema = require('./coach-schema');
const identitySchema = require('./identity-schema');
const requestSchema = require('./request-schema');

module.exports = {
  coachSchema,
  identitySchema,
  requestSchema,
};
